---
summary: "CLI reference for `friday uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `friday uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
friday uninstall
friday uninstall --all --yes
friday uninstall --dry-run
```

